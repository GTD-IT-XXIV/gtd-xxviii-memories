# Backend
backend/.venv/
backend/data/
backend/__pycache__/
backend/**/__pycache__/
*.pyc
.env

# Frontend
frontend/node_modules/
frontend/dist/

# Web (Next.js)
web/node_modules/
web/.next/
web/out/
web/.vercel/
web/*.tsbuildinfo
web/.env*
!web/.env.example

# OS
.DS_Store
Thumbs.db

# Local Claude Code state (lock files etc, machine-local)
/.claude/

# Personal photo test fixtures (270+ people's reference photos, a friend's event
# photos) - never belongs in version control.
/committee/
/TRIAL 2/
/Self Portrait (File responses)/

# Real student PII (names, Telegram handles, photo uploads) - never belongs in
# version control regardless of filename; also covers Excel's own lock files.
*.xlsx
~$*.xlsx
